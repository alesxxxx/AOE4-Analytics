/**
 * The live AoE4 patch the app's curated data (landmarks, variant rosters, civ
 * meta, build orders) is verified against. Shown as a badge so it's clear the
 * data is current. Bump this whenever the curated data is refreshed to a new patch.
 */
export const DATA_PATCH = '16.2'
